[build]
# Definir el lenguaje y entorno (Laravel está basado en PHP)
builder = "heroku/php"


[run]
# Aquí puedes definir cómo se ejecuta tu aplicación
start = "php artisan serve --host=0.0.0.0 --port=8000"
